# MVP Spec

The MVP should support:
- basic frontend shell
- backend health and API surface foundation
- CPU-only object detection pipeline in later work orders

It should not require:
- GPU acceleration
- Docker
- Redis before Alpha
- custom training
